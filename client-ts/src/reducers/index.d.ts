import { HeroStore } from '../interfaces/iStore/HeroStore';
import { HeroActionsType } from '../actions/types/HeroTypes';
export default function reducer(state: HeroStore, action: HeroActionsType): HeroStore;
